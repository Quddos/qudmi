# torch is intentionally NOT listed here — install it separately first, see
# docs/GETTING_STARTED.md, because the correct wheel depends on your GPU/CUDA setup
# (the default PyPI wheel is CPU-only and silently gives you no GPU acceleration).
numpy
scipy
tqdm
tensorboard
onnx
onnxruntime
pytest
